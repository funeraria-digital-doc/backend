name: Build and Deploy to gcp
on: [workflow_dispatch, push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout
          uses: actions/checkout@v4

        # - name: Set up Docker Buildx
        #   uses: docker/setup-buildx-action@v3
  
        - name: Authenticate to Google Cloud
          uses: google-github-actions/auth@v1
          with:
            credentials_json: ${{ secrets.GCP_KEY }}
        
        - name: Configure auth
          run: |
            gcloud auth configure-docker

        - name: get docker tag
          run: |
            IMAGE_TAGS=$(gcloud container images list-tags ${{ secrets.GCP_GCR }} --limit=1 --sort-by=~timestamp --format='get(tags)')
            echo "IMAGE_TAGS=$IMAGE_TAGS" >> $GITHUB_ENV
            echo IMAGE_TAGS

        # - name: docker build
        #   run: |
        #     docker build -t ${{ secrets.GCP_GCR }}:v1.1.1 .
        
        # - name: docker push
        #   run: |
        #     docker push ${{ secrets.GCP_GCR }}:v1.1.1
  
        # - name: Build and push
        #   uses: docker/build-push-action@v5
        #   with:
        #     context: .
        #     push: true
        #     tags: ${{ secrets.GCP_GCR }}:v10.0.0 
        #     cache-from: type=gha
        #     cache-to: type=gha,mode=max 