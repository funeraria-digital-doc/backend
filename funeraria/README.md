# funeraria-backend

Para levantar o projeto - docker compose up --build --remove-orphans

se quiseres usar detach mode acrescenta ao comando anterior -d

Para consultar a BD usar NoSQLBooster, criar nova connection e usar mongodb://username:password@localhost:27017 como connection string