version: '3.1'
name: Funeraria
services:
  web:
    image: api
    build: 
      context: .
      dockerfile: Dockerfile
    command: /bin/sh "./project_start.sh"
    ports:
      - "9000:9000"
    env_file:
      - .env
    